<%= form_with(model: integration, local: true) do |form| %>
  <% if integration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(integration.errors.count, "error") %> prohibited this integration from being saved:</h2>

      <ul>
      <% integration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id_id %>
    <%= form.text_field :user_id_id, id: :integration_user_id_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :integration_name %>
  </div>

  <div class="field">
    <%= form.label :key %>
    <%= form.text_field :key, id: :integration_key %>
  </div>

  <div class="field">
    <%= form.label :details %>
    <%= form.text_field :details, id: :integration_details %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
